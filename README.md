# 🎮 DeftHack для Unturned

Современный чит для Unturned с ImGui интерфейсом.

## 🚀 Быстрый старт

### 1. Компиляция
```batch
BUILD.bat
```

### 2. Инжекция
1. Запустите **Unturned**
2. Запустите **Inject.bat** от администратора
3. В игре нажмите **INSERT** для открытия меню

## 📁 Структура проекта

```
DeftHack-Unturned-OFFICIAL-master/
├── DeftHack/                    # Основной код чита (C#)
├── DeftHack_ImGui/             # ImGui GUI интерфейс (C++)
│   ├── main.cpp                # Точка входа DLL
│   ├── renderer.hpp            # DX11 хук
│   ├── deffhack_gui.hpp        # GUI интерфейс
│   └── imgui/                  # ImGui библиотека
├── libs/                       # Unity библиотеки
├── DeftHack_Modern.csproj      # C# проект
├── Injector.csproj             # Инжектор
├── BUILD.bat                   # Компиляция всего проекта
├── Inject.bat                   # Инжекция DLL
└── TEST_GUI.bat                # Тест GUI
```

## 🎨 GUI функции

- **LEGIT** - Легитные функции (aimbot, triggerbot)
- **VISUALS** - Визуальные функции (ESP, chams)
- **MISC** - Разное (bhop, flight, speedhack)
- **SETTINGS** - Настройки

## ⌨️ Горячие клавиши

- **INSERT** - Открыть/закрыть меню
- **END** - Выгрузить читы (в настройках)

## 🔧 Требования

- **.NET SDK 6.0+** (для C# проекта)
- **Visual Studio 2019/2022** с C++ поддержкой (для ImGui)
- **Windows SDK 10.0+**
- **DirectX 11 SDK**

## 📝 Компиляция ImGui DLL

### ⚠️ Обновление: Теперь с Kiero + MinHook!

Проект обновлен для использования **Kiero** + **MinHook** для более стабильного хука DX11.

**Быстрая установка:**

1. **Установите MinHook**:
   ```batch
   cd DeftHack_ImGui
   SETUP_MINHOOK.bat
   ```

2. **Скомпилируйте DLL**:
   ```batch
   COMPILE_WITH_KIERO.bat
   ```

3. **Инжектируйте** `version.dll` в игру

**Подробная инструкция**: `DeftHack_ImGui/ИНСТРУКЦИЯ_KIERO.md`

### Альтернатива: Использовать только C# чит

Если не хотите устанавливать Visual Studio, используйте только C# часть:
```batch
BUILD.bat
Inject.bat
```

C# чит работает без ImGui меню, но имеет все основные функции.

## ⚠️ Важно

- Используйте только на приватных серверах
- BattlEye может блокировать инжекцию
- Запускайте инжектор от администратора
