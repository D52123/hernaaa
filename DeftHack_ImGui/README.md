# 🎮 DeftHack ImGui - C++ GUI модуль

Профессиональный ImGui интерфейс для DeftHack с DX11 хуком.

## 📋 Требования

- **Visual Studio 2019+** с C++ поддержкой
- **Windows SDK 10.0+**
- **DirectX 11 SDK**
- **ImGui** (включен в проект)
- **MinHook** (опционально, для хука Present)

## 🔧 Компиляция

### Способ 1: Visual Studio
1. Откройте `DeftHack_ImGui.sln`
2. Выберите конфигурацию `Release x64`
3. Build → Build Solution

### Способ 2: CMake
```bash
mkdir build
cd build
cmake ..
cmake --build . --config Release
```

## 📁 Структура

```
DeftHack_ImGui/
├── main.cpp              # Точка входа DLL
├── renderer.hpp          # DX11 хук и рендеринг
├── deffhack_gui.hpp      # ImGui меню
├── imgui/                # ImGui библиотека
│   ├── imgui.cpp
│   ├── imgui_impl_dx11.cpp
│   ├── imgui_impl_win32.cpp
│   └── ...
└── CMakeLists.txt        # CMake конфигурация
```

## 🚀 Инжекция

### С помощью GH Injector
1. Запустите Unturned
2. Откройте GH Injector
3. Выберите процесс `Unturned.exe`
4. Выберите `DeftHack_ImGui.dll`
5. Нажмите "Inject"

### С помощью Manual Map (рекомендуется)
Используйте существующий инжектор из C# проекта, но для C++ DLL.

## ⌨️ Горячие клавиши

- **INSERT** - Открыть/закрыть меню
- **F1** - (резерв, если нужно)

## 🎨 Особенности

- ✅ Темная тема в стиле Neverlose/OTC
- ✅ Анимированные переключатели (Switch)
- ✅ Вкладки: Legit, Visuals, Misc, Settings
- ✅ Перетаскивание окна
- ✅ Полная интеграция с DX11

## ⚠️ Важно

Этот модуль работает **параллельно** с C# читом. Нужно синхронизировать переменные через:
- Shared Memory
- Named Pipes
- IPC

Или использовать только C++ модуль для всего функционала.

## 🔗 Интеграция с C# читом

Для связи между C++ и C# используйте:
1. **Shared Memory** - для обмена переменными
2. **Named Pipes** - для команд
3. **File I/O** - временные файлы (медленно)

Пример синхронизации будет добавлен позже.
