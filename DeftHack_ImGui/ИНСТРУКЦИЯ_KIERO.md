# 🎮 DeftHack ImGui с Kiero + MinHook

## ✅ Что изменилось

Проект обновлен для использования **Kiero** + **MinHook** вместо ручного хука Present.

### Преимущества:
- ✅ Более стабильный хук DX11
- ✅ Автоматическое определение SwapChain
- ✅ Поддержка разных версий DirectX
- ✅ Меньше вероятность детекта античитом

## 📋 Требования

1. **Visual Studio 2022 Community** (бесплатно)
   - Desktop development with C++
   - Windows 10 SDK

2. **Git** (для скачивания MinHook)
   - https://git-scm.com/download/win

3. **ImGui** (уже включен в проект)

4. **Kiero** (уже включен в проект)

5. **MinHook** (нужно скачать)

## 🔧 Установка и компиляция

### Шаг 1: Установите MinHook

```batch
SETUP_MINHOOK.bat
```

Этот скрипт автоматически скачает MinHook из GitHub.

**Или вручную:**
1. Скачайте: https://github.com/TsudaKageyu/minhook/archive/refs/heads/master.zip
2. Распакуйте в: `kiero-1.2.12\minhook\`

### Шаг 2: Скомпилируйте DLL

```batch
COMPILE_WITH_KIERO.bat
```

Или используйте CMake:
```batch
cmake -S . -B build
cmake --build build --config Release
```

### Шаг 3: Инжектируйте в игру

1. Запустите **Unturned**
2. Инжектируйте `version.dll` через **GH Injector**
3. Нажмите **INSERT** в игре

## 📁 Структура проекта

```
DeftHack_ImGui/
├── main.cpp                    # Точка входа с Kiero
├── deffhack_gui.hpp            # ImGui интерфейс
├── renderer.hpp                # Старый рендерер (не используется)
├── renderer_kiero.hpp          # Новый рендерер с Kiero
├── imgui/                      # ImGui библиотека
│   ├── imgui.cpp
│   ├── imgui_impl_dx11.cpp
│   └── imgui_impl_win32.cpp
├── kiero-1.2.12/              # Kiero хук библиотека
│   ├── kiero.h                # ✅ D3D11 = 1, MinHook = 1
│   ├── kiero.cpp
│   └── minhook/               # MinHook (скачивается отдельно)
│       ├── include/
│       │   └── MinHook.h
│       └── src/
│           ├── buffer.c
│           ├── hook.c
│           └── trampoline.c
├── CMakeLists.txt             # CMake конфигурация
├── SETUP_MINHOOK.bat          # Скрипт установки MinHook
└── COMPILE_WITH_KIERO.bat     # Скрипт компиляции
```

## ⚙️ Настройки Kiero

В файле `kiero-1.2.12/kiero.h` включены:

```cpp
#define KIERO_INCLUDE_D3D11  1  // ✅ Включен D3D11
#define KIERO_USE_MINHOOK    1  // ✅ Включен MinHook
```

Это **обязательно** для работы хука!

## 🎨 Функции GUI

- **LEGIT** - Aimbot, FOV, Smoothness
- **VISUALS** - ESP (Box, Skeleton, Names, Distance), Chams
- **MISC** - God Mode, No Clip, Flight, Bunny Hop, Speed Hack
- **SETTINGS** - Информация о чите, выгрузка

## ⌨️ Горячие клавиши

- **INSERT** - Открыть/закрыть меню
- **END** - Выгрузить чит (в настройках)

## 🐛 Решение проблем

### MinHook не найден

```
[✗] MinHook не найден!
```

**Решение:** Запустите `SETUP_MINHOOK.bat`

### Ошибка компиляции: kiero.h not found

**Причина:** Неправильные пути в CMakeLists.txt

**Решение:** Убедитесь что в CMakeLists.txt есть:
```cmake
target_include_directories(DeftHack_ImGui PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/kiero-1.2.12
    ${CMAKE_CURRENT_SOURCE_DIR}/kiero-1.2.12/minhook/include
)
```

### Меню не появляется в игре

**Возможные причины:**
1. Игра использует DX12 вместо DX11
2. Античит блокирует хук
3. DLL не инжектирована правильно

**Решение:**
1. Запустите игру в оконном режиме
2. Отключите Steam Overlay
3. Используйте Manual Map инжекцию
4. Проверьте что игра использует DX11

### Краш при инжекции

**Причина:** Неправильная разрядность или поврежденная DLL

**Решение:**
1. Убедитесь что DLL x64 (не x86)
2. Пересоберите проект в Release режиме
3. Отключите антивирус временно

## 📊 Сравнение с предыдущей версией

| Функция | Старая версия | Новая версия (Kiero) |
|---------|---------------|----------------------|
| Хук Present | Ручной vtable hook | Kiero + MinHook |
| Стабильность | Средняя | Высокая |
| Совместимость | DX11 только | DX9/10/11/12 |
| Детект | Выше | Ниже |
| Сложность | Высокая | Низкая |

## 🔄 Обновление с предыдущей версии

Если у вас была старая версия:

1. Удалите старый `renderer.hpp` (или переименуйте)
2. Скачайте MinHook: `SETUP_MINHOOK.bat`
3. Обновите `kiero.h` (уже обновлен)
4. Пересоберите проект: `COMPILE_WITH_KIERO.bat`

## 📞 Дополнительная информация

- **Kiero GitHub**: https://github.com/Rebzzel/kiero
- **MinHook GitHub**: https://github.com/TsudaKageyu/minhook
- **ImGui GitHub**: https://github.com/ocornut/imgui

---

**Удачи! 🎮**

Теперь у вас стабильный и надежный ImGui хук с Kiero + MinHook!
