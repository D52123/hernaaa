# 📖 DeftHack - Инструкция

## 🚀 Быстрый старт

### 1. Компиляция
```batch
BUILD.bat
```
Этот скрипт скомпилирует:
- ✅ C# проект (DeftHack_Modern.csproj)
- ✅ ImGui GUI (если установлен Visual Studio)
- ✅ Инжектор (Injector.csproj)

### 2. Инжекция
1. **Запустите Unturned**
2. **Запустите `Inject.bat` от администратора**
3. **В игре нажмите INSERT** для открытия меню

## 📁 Структура проекта

```
DeftHack-Unturned-OFFICIAL-master/
├── BUILD.bat                    # Компиляция всего проекта
├── Inject.bat                   # Инжекция DLL
├── TEST_GUI.bat                 # Тест GUI
├── CLEAN.bat                    # Очистка временных файлов
│
├── DeftHack/                    # Основной код чита (C#)
├── DeftHack_ImGui/              # ImGui GUI интерфейс (C++)
│   ├── main.cpp                 # Точка входа DLL
│   ├── renderer.hpp             # DX11 хук
│   ├── deffhack_gui.hpp         # GUI интерфейс
│   └── imgui/                   # ImGui библиотека
│
├── DeftHack_Modern.csproj       # C# проект
├── Injector.csproj              # Инжектор
├── load.cs                      # Точка входа C# чита
│
└── libs/                        # Unity библиотеки
```

## 🎨 GUI функции

### Вкладки:
- **LEGIT** - Легитные функции (aimbot, triggerbot)
- **VISUALS** - Визуальные функции (ESP, chams)
- **MISC** - Разное (bhop, flight, speedhack)
- **SETTINGS** - Настройки

### Горячие клавиши:
- **INSERT** - Открыть/закрыть меню
- **END** - Выгрузить читы (в настройках)

## 🔧 Компиляция ImGui DLL вручную

Если автоматическая компиляция не работает:

### Способ 1: Visual Studio
1. Откройте Visual Studio 2019/2022
2. File → Open → CMake...
3. Выберите `DeftHack_ImGui\CMakeLists.txt`
4. Build → Build All (F7)
5. Скопируйте `DeftHack_ImGui\build\Release\DeftHack_ImGui.dll` в корень проекта

### Способ 2: CMake командная строка
```batch
cd DeftHack_ImGui
mkdir build
cd build
cmake .. -G "Visual Studio 17 2022" -A x64
cmake --build . --config Release
copy Release\DeftHack_ImGui.dll ..\..\DeftHack_ImGui.dll
```

## ⚠️ Требования

- **.NET SDK 6.0+** (для C# проекта)
- **Visual Studio 2019/2022** с C++ поддержкой (для ImGui)
- **Windows SDK 10.0+**
- **DirectX 11 SDK**

## 🧹 Очистка

Для удаления временных файлов:
```batch
CLEAN.bat
```

## ⚠️ Важно

- Используйте только на приватных серверах
- BattlEye может блокировать инжекцию
- Запускайте инжектор от администратора
- Если меню не появляется, проверьте логи инжектора
